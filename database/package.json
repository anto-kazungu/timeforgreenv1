{
  "name": "justgogreen-database",
  "version": "1.0.0",
  "description": "PostgreSQL database integration for Just Go Green",
  "main": "connection-config.ts",
  "scripts": {
    "setup": "psql -U justgogreen_user -d justgogreen -f schema.sql",
    "seed": "psql -U justgogreen_user -d justgogreen -f seed-data.sql",
    "reset": "npm run drop && npm run setup && npm run seed",
    "drop": "psql -U postgres -c 'DROP DATABASE IF EXISTS justgogreen' && psql -U postgres -c 'CREATE DATABASE justgogreen'",
    "test-connection": "ts-node test-connection.ts",
    "import-data": "ts-node import-to-postgres.ts",
    "backup": "pg_dump -U justgogreen_user justgogreen > backup_$(date +%Y%m%d_%H%M%S).sql",
    "migrate": "npm run setup && npm run import-data"
  },
  "keywords": [
    "postgresql",
    "database",
    "justgogreen",
    "environmental"
  ],
  "author": "Just Go Green Team",
  "license": "MIT",
  "dependencies": {
    "pg": "^8.11.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/pg": "^8.10.9",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  }
}
